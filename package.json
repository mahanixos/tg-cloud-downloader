{
  "name": "tg-cloud-downloader",
  "version": "0.1.0",
  "description": "Telegram -> Cloud Storage -> Direct Download system (MVP bootstrap, Phase 1)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "grammy": "^1.46.0",
    "pino": "^9.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
